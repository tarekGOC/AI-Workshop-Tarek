version: '3.8'

services:
  rust-app:
    build: .
    container_name: rust-skeleton
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - RUST_LOG=info
    restart: unless-stopped
